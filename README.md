# Interior Configuration Prototype

Responsive HTML prototype for configuring apartment interior packages, materials, optional services, and an indicative estimate.

## GitHub Pages

This repository includes a Pages deployment workflow. In repository Settings → Pages, select **GitHub Actions** as the source if it is not already enabled.
